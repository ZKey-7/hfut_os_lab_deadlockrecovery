OS课设-死锁解除(deadlock recovery)。
结果经检验，符合预期。

实现了
1.文件输入输出资源分配图。

2.死锁检测算法,可正确判断有无出现死锁的情况。

3.死锁解除，出现死锁后，可以可以通过深度(dfs),广度(bfs)两种方式进行死锁解除。
  算法每一步会输出杀死节点的过程。和资源分配情况。
  最终输出最佳死锁解除序列。

4.自动随机生成一个带死锁的资源分配图，并能保存到文件中。进行读取。
  方便测试

注意事项：
1.权重定义：进程权重只与进程占有，请求资源数有关。
2.分配策略：分配策略采用保守策略，等待死锁解除后才分配资源。因此在这期间权重不变。
